name: ISR Lockfile Refresh

on:
  push:
    branches:
      - ideas
    paths:
      - '.github/workflows/lock-refresh.yml'

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ideas
        uses: actions/checkout@v6
        with:
          ref: ideas

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Refresh backend lockfile
        working-directory: backend
        run: npm install --package-lock-only --ignore-scripts

      - name: Refresh frontend lockfile
        working-directory: frontend
        run: npm install --package-lock-only --ignore-scripts

      - name: Verify generated lockfiles
        run: git diff --check -- backend/package-lock.json frontend/package-lock.json

      - name: Commit refreshed lockfiles
        shell: bash
        run: |
          if git diff --quiet -- backend/package-lock.json frontend/package-lock.json; then
            echo "Lockfiles are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/package-lock.json frontend/package-lock.json
          git commit -m "Refresh dependency lockfiles"
          git push origin HEAD:ideas
